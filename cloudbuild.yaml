steps:
- id: 'validator binary'
  name: 'gcr.io/cloud-builders/docker:18.09.6'
  entrypoint: 'bash'
  args: 
  - '-c'
  - |
  
      chmod 755 terraform-validator-linux-amd64;
      stat="$(./terraform-validator-linux-amd64 validate consolidated/cloudresources/tfplan900.tfplan.json --policy-path=consolidated/policy-library/ --project=$PROJECT_ID )"   
   
      if [ $stat -eq 2 ]; then
           echo "exception found"
           #exit 1
       fi
       #chmod 755 terraform-validator-linux-amd64; ./terraform-validator-linux-amd64 validate consolidated/cloudresources/tfplan900.tfplan.json --policy-path=consolidated/policy-library/ --project=$PROJECT_ID >> /workspace/result.txt  
